{
  "name": "brewski-bets",
  "version": "1.5.1",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others --names \"tunnel,api,web\" --prefix-colors \"yellow,green,cyan\" \"pnpm -C backend tunnel\" \"pnpm -C backend dev\" \"pnpm -C frontend start\"",
    "dev:tunnel": "pnpm -C backend tunnel",
    "dev:api": "pnpm -C backend dev",
    "dev:web": "pnpm -C frontend start",
    "install:all": "pnpm install && pnpm -C backend install && pnpm -C frontend install"
  },
  "devDependencies": {
    "concurrently": "10.0.3"
  },
  "packageManager": "pnpm@10.32.1"
}
